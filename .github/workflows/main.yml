name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: executing remote ssh commands using password
      run: git clone git@github.com:digital-volki/h-api.git && cd h-api && docker-compose up -d
